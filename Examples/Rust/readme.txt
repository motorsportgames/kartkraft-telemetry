To listen for telemetry, open a terminal, build and run:

cd .\listen_for_telemetry
cargo build
cargo run

To send some example telemetry, open a terminal, build and run:

cd .\send_telemetry
cargo build
cargo run

